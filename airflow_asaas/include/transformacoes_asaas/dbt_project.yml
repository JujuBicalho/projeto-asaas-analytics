name: 'transformacoes_asaas'
version: '1.0.0'
config-version: 2

profile: 'transformacoes_asaas'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  transformacoes_asaas:
    staging:
      +materialized: view
    marts:
      +materialized: table